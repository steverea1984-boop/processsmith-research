# Copy to ".env" (same folder) and fill in. The real .env is gitignored — never commit it.
# The harness loads this automatically; shell-exported vars take precedence.

SAKANA_API_KEY=
ANTHROPIC_API_KEY=
